import { Box, styled, th } from '@smooth-ui/core-em'

const H2Anchor = styled(Box)`
  text-decoration: none;
  display: block;
  color: ${th('gray700')};
  font-size: 14px;
`

export default H2Anchor
