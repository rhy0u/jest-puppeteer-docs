import { Box, styled, th } from '@smooth-ui/core-em'

const H3Anchor = styled(Box)`
  text-decoration: none;
  display: block;
  color: ${th('gray700')};
  font-size: 12px;
`

export default H3Anchor
