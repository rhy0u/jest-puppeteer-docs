import { theme as defaultTheme } from '@smooth-ui/core-em'

const theme = {
  ...defaultTheme,
  primary: '#bd4932',
  lightgrey: '#EDF1F5',
  fontFamily: "'Open Sans', sans-serif",
  h2FontSize: '18px',
}

export default theme
